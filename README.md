# web2swagger
Extract a websites API schema quickly using MITMProxy.

If you're using this code on a Github Codespace, simply run the ```setup.sh``` script to have everything set up for you.

I'm working on a version to generate documentation without AI, but using an LLM is a good start for now. I'll add options for different providers in the future, but in its current state it is OpenAI compatible.